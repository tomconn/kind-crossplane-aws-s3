# Step 1: Install the AWS Provider package into the cluster.
# This must be done FIRST to install the necessary CRDs.
apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-aws-s3
spec:
  package: xpkg.crossplane.io/crossplane-contrib/provider-aws-s3:v1.21.1